package com.example.demo.dto;

public class CrsList {
    // Define fields corresponding to each key-value pair in the "crsList" array
    // You can add these fields similar to the other classes above
}
